# 布道师学习通单体应用配置
# Copyright 2026 BudaOS Team
# Licensed under the Apache License, Version 2.0

server:
  port: 9080
  servlet:
    context-path: /
  tomcat:
    uri-encoding: UTF-8
    max-threads: 200

spring:
  profiles:
    active: dev
  application:
    name: budaos-monolith

  # 数据源配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/budaos?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: budaos
    # Druid连接池配置
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      filters: stat,wall,log4j2
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000

  # Redis配置
  redis:
    host: localhost
    port: 6379
    password: budaos
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 3000ms

  # Session配置
  session:
    store-type: redis
    redis:
      namespace: budaos:session
    timeout: 3600m

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  # jackson配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

# MyBatis配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.budaos.common.domain,com.budaos.modules.sys.domain,com.budaos.modules.evgl.**
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    cache-enabled: true

# PageHelper分页配置
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

# 日志配置
logging:
  level:
    root: INFO
    com.budaos: DEBUG
    com.budaos.modules: DEBUG
    org.apache.tomcat.util.compat: WARN  # 隐藏Tomcat JDK兼容性探测日志
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n'
  file:
    name: logs/budaos.log
    max-size: 100MB
    max-history: 30

# 应用配置
com.budaos:
  # 文件上传路径
  file-upload-path: /mnt/budaos/uploads
  # 文件访问路径
  file-access-path: /uploads
  # 允许跨域
  is-cors: true
  cors-url: "*"
  # Redis标志
  redisFlag: false
  # MQ类型
  mq-type: redis
  # 基础URL
  baseUrl: http://localhost:9080
  # 编辑器图片路径
  cbeditor-image-path: cbeditor-image
  cbeditor-image-suffix:
  cbeditor-media-path: cbeditor-media
  cbeditor-media-suffix:
  cbeditor-file-path: cbeditor-file
  cbeditor-file-suffix:
  # 上传路径配置
  cb-upload-paths: default
  # 第三方登录
  loginBackUrl:
  defaultHeadImg:
  defaultName:
  qqAppId:
  qqAppKey:
  wxAppId:
  wxAppKey:
  xlAppId:
  xlAppKey:
  gitUrl:
  gitTicket:

# 微信支付配置
weixin:
  appid: wx782efe0306206d7b                         # 小程序appid
  app_appid: xx                     # APP端appid
  secret:                         # 小程序秘钥
  mchid:                          # 商户号
  partner:                        # 商户号(兼容旧版)
  partnerkey:                     # 商户秘钥(兼容旧版)
  key:                            # 商户秘钥
  notifyurl:                      # 通知地址
  pc_notifyurl:                   # PC端回调地址
  app_notifyurl:                  # APP端回调地址
  pc_refund_notifyurl:            # PC退款回调地址
  order_refund_notifyurl:         # 订单退款回调地址
  pc_bond_refund_notifyurl:       # PC保证金退款回调地址
  certurl:  /cert/apiclient_cert.p12                        # 证书路径
  certLocation:                   # 证书位置(兼容旧版)
  redirect_url:                   # H5支付跳转地址
  paymentAmount:                  # 支付金额配置

# OAuth2 安全配置
security:
  oauth2:
    client:
      access-token-uri: http://localhost:9080/oauth/token
      logout: http://localhost:9080/oauth/logout
      client-id: app
      client-secret: app

budaos:
  # 文件上传路径
  upload-path: uploads/
  # 验证码开关
  captcha-enabled: true
  # 单点登录开关（单体应用设为false）
  sso-enabled: false
  # FastDFS配置
  fastdfs:
    tracker-servers: 127.0.0.1:22122
    connect-timeout: 5
    network-timeout: 30
